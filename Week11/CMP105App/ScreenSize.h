#pragma once
#include <SFML/Graphics.hpp>

class ScreenSize
{
public:
	GetWindow


private:

	sf::RenderWindow* window = null;

	static float m_screen_width = window.getSize().x;
	static float m_screen_height = window.getSize().y;


};

